# =============================================================================
# GestorFinanceiro - Configurações de Ambiente
# =============================================================================
# Copie este arquivo para `.env` e configure as variáveis conforme necessário
# Para produção, gere valores seguros para as credenciais e secrets

# -----------------------------------------------------------------------------
# Web Server (Frontend)
# -----------------------------------------------------------------------------
# Porta do host onde a aplicação web será acessível
# Padrão: 8080 (acesse http://localhost:8080)
WEB_PORT=8080

# -----------------------------------------------------------------------------
# PostgreSQL Database
# -----------------------------------------------------------------------------
# Nome do banco de dados
POSTGRES_DB=gestorfinanceiro

# Usuário do banco de dados
POSTGRES_USER=postgres

# Senha do banco de dados
# ⚠️ PRODUÇÃO: Altere para uma senha forte e segura
POSTGRES_PASSWORD=postgres

# -----------------------------------------------------------------------------
# JWT Authentication
# -----------------------------------------------------------------------------
# Secret key para assinatura dos tokens JWT
# ⚠️ OBRIGATÓRIO: Deve ter no mínimo 32 bytes (256 bits) para HMAC-SHA256
# ⚠️ PRODUÇÃO: Gere uma chave criptograficamente segura
# Exemplo de geração Linux/Mac: openssl rand -base64 32
# Exemplo de geração PowerShell: [Convert]::ToBase64String((1..32|%{Get-Random -Maximum 256}))
JWT_SECRET=CHANGE_THIS_TO_A_SECURE_32_BYTE_MINIMUM_SECRET_KEY_FOR_PRODUCTION

# -----------------------------------------------------------------------------
# Admin User Seed (Primeiro Acesso)
# -----------------------------------------------------------------------------
# Nome do usuário administrador inicial
ADMIN_NAME=Administrador

# Email do usuário administrador inicial
# ⚠️ Este será o login padrão - altere para seu email
ADMIN_EMAIL=admin@gestorfinanceiro.local

# Senha do usuário administrador inicial
# ⚠️ IMPORTANTE: Altere esta senha no primeiro login
# ⚠️ PRODUÇÃO: Use uma senha forte ou configure via secrets management
ADMIN_PASSWORD=mudar123

# -----------------------------------------------------------------------------
# Observability (Opcional)
# -----------------------------------------------------------------------------
# Endpoint para envio de telemetria OpenTelemetry (deixe vazio se não usar)
OTEL_ENDPOINT=
